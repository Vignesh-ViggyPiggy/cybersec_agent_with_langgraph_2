# LangChain and LangGraph
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langgraph>=0.2.0

# Pydantic for structured outputs
pydantic>=2.0.0

# DuckDuckGo search
ddgs>=9.10.0

# Optional but recommended
python-dotenv>=1.0.0

# MCP file transfer — this machine acts as an MCP *client* toward the vault's
# mcp_server.py (pulling/pushing hierarchy files) and an MCP *server* itself
# via trigger_mcp_server.py (accepting "analyze this hierarchy" requests) and
# corpus_server.py (the attack-corpus vector store, same client/server pattern).
fastmcp>=2.0.0

# Local vector store for the attack corpus (corpus_server.py). Embeddings use
# Ollama's nomic-embed-text model (requires: ollama pull nomic-embed-text),
# not chromadb's own default embedding function, so this stays inside the
# one local runtime already used everywhere else in this project.
chromadb>=0.5.0
