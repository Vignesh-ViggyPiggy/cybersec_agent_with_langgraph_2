{
  "id": "attack_weak_password_accounts",
  "explanation": "Empty-password account audit. Usr_info -ep dumps accounts with an empty password field directly from /etc/shadow. Unlike SUID binaries or open ports, there is no legitimate reason for ANY account to have an empty password on a production system -- a count of exactly zero is the only acceptable state, making this one of the cleanest 'any occurrence is bad' checks in the whole bundle.",
  "metadata": {
    "attack_type": "weak_password_accounts",
    "status_file": "athinio/system/user_emptypass_list.xml",
    "status_tag": "NoOfAccounts",
    "writer_script": "Usr_info -ep (via largefiles.py)",
    "confidence": "script mechanism verified (reads /etc/shadow directly); exact output filename and tag name are inferred from the temp-file naming pattern (/EMPTYPASS), not directly observed in a real XML file",
    "verification_category": "opaque_binary_only",
    "value_schema": {"type": "count_greater_than_zero"},
    "value_meanings": {
      "0": "Clean -- no accounts with an empty password field.",
      "N (N>=1)": "N accounts have no password set -- this is always a finding worth acting on immediately, not something to triage by count; even N=1 is a live, trivially exploitable account."
    },
    "caveat": "Exact status_file/status_tag names are inferred, not confirmed against a real populated example.",
    "raw_content_examples": [
      {"label": "clean (reconstructed)", "content": "<EmptyPasswordAccounts>\n  <NoOfAccounts>0</NoOfAccounts>\n</EmptyPasswordAccounts>", "note": "SPECULATIVE format -- exact schema not directly observed."},
      {"label": "detected (reconstructed)", "content": "<EmptyPasswordAccounts>\n  <NoOfAccounts>1</NoOfAccounts>\n  <Account1>testuser</Account1>\n</EmptyPasswordAccounts>", "note": "SPECULATIVE -- illustrates the likely intent based on the script reading /etc/shadow directly, not a confirmed real schema."}
    ]
  }
}
